<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 text-bold mb-6 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5 ">Edit Your Post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="Write Whatever In Your Mind Sir..." class="p-3 outline-none w-1/3 h-36 resize-none bg-transparent border-2 border-zinc-800 rounded-md block" name="content"><%= post.content %></textarea>
            <input class="w-40 px-3 py-2 bg-yellow-500 block rounded-md mt-4 font-bold" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>